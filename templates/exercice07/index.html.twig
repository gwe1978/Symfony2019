{% extends 'base.html.twig' %}

{% block title %}
    Liste des étudiants
{% endblock %}

{% block body %}
    <h1>Exercice 07</h1>
    <ul>
        {% for section in tab_sections %}
            <li>{{ section.nom }}</li>
        {% endfor %}
    </ul>

    <table>
        {% for etudiant in tab %}
            <tr>
                <td>{{ etudiant.nom }}</td>
                <td>{{ etudiant.prenom }}</td>
                <td>{{ etudiant.section.nom }}</td>
            </tr>
        {% endfor %}
    </table>
{% endblock %}